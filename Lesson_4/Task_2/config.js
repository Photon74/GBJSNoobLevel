'use strict';
/**
 * Объект с настройками игры
 * @property {int} rowsCount - кол-во  строк на карте
 * @property {int} colsCount - кол-во  колонок на карте
 */
const config = {
    rowsCount: 10,
    colsCount: 10,
};